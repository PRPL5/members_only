<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
  <style>
    body{
      padding: 0;
      margin: 0;
      background-color: #24292e;
            font-family: Arial, Helvetica, sans-serif;

    }
    .content{
      width: 100%;
      height: 100%;
      display: flex;
      flex-direction:column ;
align-items: center;
justify-content: center;
    }
    .header{
      display: flex;
    }

    .header > h1{
font-size: 48px;
color:  #fafbfc;
    }


    .messages{
      display: flex;
       flex-direction: column;
       gap: 12px;

    }.message{
      border: 1px solid magenta;
      display: flex;
      flex-direction: column;
      width: 40rem;
      padding: 20px;
    }

    .author{
      font-weight: 700;
      color: #fafbfc;
      font-size: 24px;

    }
    .msg-text{
      color: #999999;
      font-size: 18px;
    }
    .date{
      color: #999999;
      font-size: 18px;
      place-self: end;
    }
    .new-btn{
      border: 2px solid #999999;
      padding: 10px;
    text-decoration: none;
    color: #999999;
    font-weight: 700;
      font-size: 20px;
      margin-bottom: 13px;


}

.delete{
  width: 20px;
  height: 20px;
  text-align: center;
  font-weight: bolder;
  border: none;
  background-color: inherit;
  color: red;
}
</style>

  <div class="content">
    <div class="header">
        <h1>Members Only </h1>


    </div>
    <a href="/new-msg" class="new-btn">New Message</a>
    <div class="messages">

      <% messages.forEach(msg => { %>
  <div class="message">
    <h3 class="author"><%= msg.firstname %> <%= msg.lastname %></h3><br>
    <p class="msg-text"><%= msg.content %></p>
  <% if(user && user.is_admin ){%>
<a href="/delete-msg/<%= msg.id %>" class="delete">X</a>
<% }%>
    <p class="date"><%= msg.created_at.toLocaleDateString() %></p>
  </div>
<% }) %>
    </div>

    </div>
</body>
</html>